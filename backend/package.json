{
  "name": "visao-total-backend",
  "version": "1.0.0",
  "private": true,
  "description": "API do sistema Visão Total — banco local (SQLite via libSQL) + eventos em tempo real (SSE).",
  "type": "commonjs",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "seed": "node src/seed.js"
  },
  "dependencies": {
    "@libsql/client": "^0.17.4",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
